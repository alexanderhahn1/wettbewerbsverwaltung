version: '3.9'

services:
  db:
    image: postgres:16-alpine
    container_name: wettbewerbs-database
    environment:
      POSTGRES_DB: wettbewerbsverwaltung
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 4DK88W61f56a17xK
    ports:
      - "5423:5432"  # host:container
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U demo"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build:
      context: ./../backend
    image: ghcr.io/pfarrhoferphilip/wettbewerbs-backend
    container_name: wettbewerbs-backend
    depends_on:
      db:
        condition: service_healthy
    environment:
      QUARKUS_DATASOURCE_JDBC_URL: jdbc:postgresql://db:5432/wettbewerbsverwaltung
      QUARKUS_DATASOURCE_USERNAME: admin
      QUARKUS_DATASOURCE_PASSWORD: 4DK88W61f56a17xK
    ports:
      - "8080:8080"
    networks:
      - backend

  frontend:
    build:
      context: ./../frontend
    image: ghcr.io/pfarrhoferphilip/wettbewerbs-frontend
    container_name: wettbewerbs-frontend
    ports:
      - "4200:80"  # Host port 4200 maps to nginx's port 80
    networks:
      - backend

networks:
  backend:
